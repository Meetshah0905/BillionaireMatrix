# BillionaireMatrix environment variables example
# Copy this file to .env and adjust values.

# Core application
NODE_ENV=development
PORT=3000

# Security
SESSION_SECRET=change_this_to_a_long_random_string

# Database (Postgres / Neon). If using in-memory only, leave unset.
# Format: postgres://user:password@host:port/database
DATABASE_URL=postgres://user:password@localhost:5432/billionaire_matrix

# Optional: External service placeholders (uncomment as needed)
# API_BASE_URL=https://api.example.com
# LOG_LEVEL=debug
# FEATURE_FLAG_EXPERIMENTAL=false
